# PLACEHOLDER — do not commit real values
# Replace with a SOPS-encrypted file or a SealedSecret before deploying to production
apiVersion: v1
kind: ConfigMap
metadata:
  name: eosa-configs
type: Opaque
stringData:
  base-url: "http://localhost:8080"
  shipping-amount: "4.90"
  shipping-free-threshold: "60.00"
