# PLACEHOLDER — do not commit real values
# Replace with a SOPS-encrypted file or a SealedSecret before deploying to production
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secrets
type: Opaque
stringData:
  username: "change_me"
  password: "change_me"
  # jdbc-url/jdbc-username/jdbc-password in eosa-secrets must use these same credentials
